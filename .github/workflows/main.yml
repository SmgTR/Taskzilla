on:
  pull_request:
    branches:
      - main
    types: [closed]

jobs:
  update_changelog:
    name: Update and Commit Changelog
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      # Checkout git.
      - uses: actions/checkout@v3
        with:
          ref: main # checkout dev branch.
          fetch-depth: 0 # fetch all history.
          
      - run: git config --global user.email "smagokpnd@gmail.com"
      - run: git config --global user.name "SmgTR"

      - name: Create Changelog Entry String
        run: |
          cl_entry="${{ github.event.pull_request.title }}  "
          cl_entry="${cl_entry//'%'/'%25'}"
          cl_entry="${cl_entry//$'\n'/'%0A'}"
          cl_entry="${cl_entry//$'\r'/'%0D'}"
          echo "CONTENT=$cl_entry" >> $GITHUB_ENV
        id: changelog

      # Update changelog.
      - run: |
          echo "${{ steps.changelog.outputs.content }}" >> changelog.md

      # Commit changes.
      - run: git add changelog.md
      - run: git commit -m "Update Changelog" || echo "No changes to commit"
      - run: git push origin main
